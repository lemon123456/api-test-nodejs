{
  "name": "api-testing",
  "version": "1.0.0",
  "description": "API testing framework using gulp mocha and supertest.",
  "main": "gulpfile.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true,
  "dependencies": {
    "chai": "^4.1.2",
    "gulp-mocha": "^2.2.0",
    "gulp-util": "^3.0.8",
    "supertest": "^3.0.0"
  },
  "devDependencies": {
    "gulp": "^3.9.1",
    "minimist": "^1.2.0",
    "mochawesome": "^2.3.1"
  }
}
